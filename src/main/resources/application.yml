spring:
  datasource:
    username: ${db_username}
    password: ${db_password}
    url: ${db_url}
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true

project:
  poster: posters/

base:
  url: "http://localhost:8080"